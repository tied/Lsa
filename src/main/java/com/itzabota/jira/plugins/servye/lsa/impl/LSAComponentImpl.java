package com.itzabota.jira.plugins.servye.lsa.impl;


/*
import javax.inject.Inject;

import com.atlassian.plugin.spring.scanner.annotation.imports.ComponentImport;
import com.atlassian.sal.api.ApplicationProperties;
import com.itzabota.jira.plugins.servye.lsa.api.LSAComponent;

@ExportAsService ({LSAComponent.class})
@Named ("lsaComponent")

*/

public class LSAComponentImpl //implements LSAComponent
{

/*
    @ComponentImport
    private final ApplicationProperties applicationProperties;

    
    
    @Inject
    public LSAComponentImpl(final ApplicationProperties applicationProperties)
    {
        this.applicationProperties = applicationProperties;
    }

    public String getName()
    {
        if(null != applicationProperties)
        {
            return "myComponent:" + applicationProperties.getDisplayName();
        }
        
        return "myComponent";
    }
    */
}